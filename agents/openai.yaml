interface:
  display_name: "Project Master 2.0"
  short_description: "Ведёт сложный проект от идеи до проверенной приёмки"
  default_prompt: "Используй $project-master, чтобы запустить или продолжить текущий проект."
policy:
  allow_implicit_invocation: true
